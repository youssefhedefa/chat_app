import 'package:flutter/material.dart';

class TimeOfMessage extends StatelessWidget {
  const TimeOfMessage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
      '2:00',
    );
  }
}
